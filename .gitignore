.DS_Store
*.swp
*.swo
.idea/
.vscode/

# 本地证据存档（不推送）
*conversation-archive*/
*对话记录*
*创作时间线*
*反抄袭证据*
*.jsonl
SHA256-校验码.txt

# wayfinder 工作区（不推送）
.scratch/

# /euro-cv 在仓库里跑时的输出目录（含真实姓名、手机号、证件照）
# 必须带前导斜杠锚定到仓库根——否则会连 skills/euro-cv/ 一起忽略掉
/euro-cv/
